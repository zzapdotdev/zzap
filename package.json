{
  "name": "zzap",
  "private": true,
  "scripts": {
    "dev": "bun run package && bunx turbo dev --filter zzap.dev --filter zzap",
    "dev:pokedex": "bun run package && bunx turbo dev --filter pokedex",
    "dev:minimal": "bun run package && bunx turbo dev --filter minimal",
    "dev:all": "bun run package && bunx turbo dev",
    "build": "bun run package && bunx turbo build",
    "test": "bunx turbo test",
    "package": "bunx turbo package",
    "build:zzap.dev": "bun run package && bunx turbo build --filter zzap.dev",
    "build:pokedex": "bun run package && bunx turbo build --filter pokedex"
  },
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "dependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.12",
    "turbo": "^1.12.5"
  }
}
