{
  "name": "zzap",
  "private": true,
  "scripts": {
    "dev": "bun run package && bunx turbo dev --filter zzap.dev --filter zzap",
    "build": "bun run package && bunx turbo build",
    "distribute": "bun run package && bunx turbo distribute",
    "test": "bunx turbo test",
    "package": "bunx turbo package",
    "patch": "bunx turbo patch",
    "minor": "bunx turbo minor",
    "major": "bunx turbo major"
  },
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "dependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.12",
    "turbo": "^1.12.5"
  }
}
